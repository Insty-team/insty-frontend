import React from 'react'
import VideoForm from '../common/VideoForm'

function VideoUpload() {
  return (
    <VideoForm subject="영상 업로드" onSubmit={() => {}} onBack={() => {}} />
  )
}

export default VideoUpload